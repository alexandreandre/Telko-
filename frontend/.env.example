# Par défaut, Vite lit aussi le fichier .env à la racine du dépôt (voir vite.config envDir).
# Tu peux aussi créer frontend/.env pour des overrides locaux.

# Backend FastAPI — optionnel en dev : laisser vide ou absent pour le proxy Vite → :8000
# VITE_API_URL=http://127.0.0.1:8000

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
